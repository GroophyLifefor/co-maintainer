{
  "name": "@murat/co-maintainer",
  "version": "0.2.13",
  "license": "MIT",
  "exports": "./mod.ts",
  "imports": {
    "@db/sqlite": "jsr:@db/sqlite@^0.13.0"
  },
  "fmt": {
    "exclude": ["PLAN.md", "z_flow/"]
  },
  "lint": {
    "rules": {
      "exclude": ["require-await"]
    }
  },
  "tasks": {
    "dev": "deno run --allow-all --watch main.ts",
    "probe": "deno run --allow-all main.ts probe",
    "init": "deno run --allow-all main.ts init",
    "remake": "deno run --allow-all main.ts remake",
    "review": "deno run --allow-all main.ts review",
    "serve": "deno run --allow-all main.ts serve",
    "bench": "deno run --allow-all benchmark/run.ts",
    "bench-rereview": "deno run --allow-all benchmark/run_rereview.ts",
    "check": "deno check main.ts",
    "fmt": "deno fmt",
    "test": "deno test --allow-all",
    "e2e": "deno run --allow-all scripts/e2e.ts"
  }
}
