---
name: GroophyLifefor-heap-analysis-maintainer
description: Repository-specific guidance for contributing to GroophyLifefor/heap-analysis. Use when writing code, opening or reviewing pull requests, debugging, or releasing this repository.
---

# GroophyLifefor/heap-analysis

Generated from repository metadata and selected project history. Prefer current repository files and workflows when this guidance conflicts with a stale example.
## Repository

- Work in GroophyLifefor/heap-analysis on the `main` default branch.
- PR #40 adds src/diff/growth.js exporting findGrowth(before, after, { top = 10 } = {}).
- PR #39 adds src/diff/object_diff.js exporting diffObjects(before, after, { top = 10 } = {}).
- Adds Snapshot#contextRetainerCountOf(nodeIndex), returning how many closures have an internal context edge to a node.
- Adds new file src/report/duplicate_strings.js exporting findDuplicateStrings(snapshot, { top }).
- New file src/graph/find_by_class_name.js exports a function named findByClassName.

## Development and debugging

- Follow repository contribution guidance in `CONTRIBUTING.md`.
- README 6. Snapshots are generated by tests, never committed: `test/helpers/fixture.js` gives you two options. `tinySnapshot()` is hand written and small enough to verify by eye, use it for anything about decoding. `withRealSnapshot()` writes a real one from the running process, use it to check invariants that a hand wri…
- README 7. Zero runtime dependencies: `package.json` has no `dependencies` and the intent is to keep it that way. Node's standard library covers what this package needs, including argument parsing (`node:util`), testing (`node:test`), and taking snapshots (`node:v8`). A dependency has to earn its …
- README Contributing: See CONTRIBUTING.md for the conventions this codebase follows and why.
- Adds a `check` command to the COMMANDS map in src/cli.js, wiring it to the new checkCommand.
- findGrowth filters diffByClass rows to sizeDelta > 0 and adds growthRatio = sizeDelta / sizeBefore.

## Shipping

- CI workflows are defined in `.github/workflows/ci.yml`, `.github/workflows/publish.yml`.
- Changes targeting pull requests run `.github/workflows/ci.yml`.
- Workflow `.github/workflows/ci.yml` defines jobs: `test`.
- Workflow `.github/workflows/publish.yml` defines jobs: `publish`.
- Use `npm run verify-pack` for node scripts/verify-pack.mjs.
- Adds renderJUnit and renderGithubActions so CI systems can consume policy results without knowing this package's JSON shape.

## Code layout

- `src/cli.js` contains `packageJson`, `COMMANDS`, `runCli`, `run`, `usage`, `names`, `commandList`; keep related changes in this module.
- `src/cli/render_table.js` contains `renderTable`, `widths`, `renderRow`, `text`, `pad`, `lines`, `row`; keep related changes in this module.
- `src/commands/check.js` contains `FAIL_ON_VALUES`, `checkCommand`, `snapshot`, `policy`, `evaluation`, `v`, `e`; keep related changes in this module.
- `src/commands/diff.js` contains `MODES`, `diffCommand`, `top`, `before`, `after`, `result`, `rows`, `renderInstanceTable`; keep related changes in this module.
- `src/commands/gc_path.js` contains `gcPathCommand`, `top`, `snapshot`, `matches`, `rows`; keep related changes in this module.
- `src/commands/retained.js` contains `retainedCommand`, `top`, `snapshot`, `idom`, `retained`, `rows`; keep related changes in this module.

## Code style

- Use conventional commit prefixes when creating commits.
- Recent commit examples: `release: v0.5.0`; `docs: export the full public api surface, rewrite readme (#50)`; `feat: mcp server and tools (#49)`; `release: v0.4.0`; `feat: policy gate exit codes, junit and github actions output (#48)`; `feat: policy rule evaluation engine (#47)`; `feat: policy file loader (#46)`; `release: v0.3.0`.
- contextRetainerCountOf lazily builds and caches an Int32Array count on first use instead of recomputing.
- The diff replaces #referrers with #firstReferrer and #referrerNodes typed arrays.
- Adds #firstEdge private field and buildEdgeOffsets helper using Int32Array of length nodeCount + 1.

## Tests

- Repository tests live under `test/`.
- Use `npm run test` for node --test "test/**/*.test.js".
- Tests cover a single closure (not reported), two closures sharing a context (count 2), top:0, and a real snapshot with 6 closures.
- withDuplicateString() mutates tinySnapshot JSON to add a duplicate 'hello' string node and a unique 'solo' node.
- Tests verify order by retained size, tie-break by nodeIndex, top option, full decoded row shape, and real snapshot descending order.
- Tests cover linear fixture, diamond graph, and a real snapshot for dominance queries.

## Pull request title and body

- Most selected pull requests contain a substantive description; explain purpose, scope, and verification before requesting review.
- Title is 'feat: object-level snapshot diff'; body says it differs from diffByClass by seeing actual objects, not aggregate totals.
- PR title uses 'feat:' prefix and body explains function is useful after constructor summary.
- Title is 'feat: dominance queries (retainer chain, containment)' and body explains both functions.
- PR title is 'feat: unreachable node accounting'.
- Body states unreachableSummary() returns { count, totalSize } for nodes not in reachableNodes().

## Labels

- PR has no labels attached.
- PR #39 has no labels, comments, or reviews.
- The PR labels array is empty in the payload.
- The PR metadata lists an empty labels array.
- The PR has no labels attached.
- The PR metadata contains an empty labels array, so no labels were attached.

## Review bar

- Frequently changed files include `src/snapshot.js`, `test/snapshot.test.js`, `index.js`, `package.json`, `src/cli.js`, `README.md`, `src/errors.js`, `test/errors.test.js`, `.gitignore`, `src/graph/retained_size.js`; check nearby tests and workflows before editing.
- PR has no comments or reviews.
- PR has no comments and no reviews.

## Pull request process

- 50/50 selected pull requests are merged; keep work mergeable before requesting review.
- PR body says it was generated with Claude Code.
- Body test plan marks npm test as done and CI runs green as unchecked.
- PR body says root names were verified by inspecting '(GC roots)'s own 27 children from real snapshots rather than assumed.
- The PR's test plan has npm test checked, but the CI runs green checkbox is unchecked.
- The PR body lists `CI runs green on this PR` as an uncompleted checkbox.
